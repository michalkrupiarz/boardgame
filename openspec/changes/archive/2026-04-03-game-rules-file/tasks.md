## 1. Create the Rules File

- [x] 1.1 Create `docs/rules.md` with the full rulebook structure (sections: Overview, Turn Structure, Resources, The Map & Tiles, Cultural Expansion, Building & Infrastructure, Victory Conditions, Edge Cases)

## 2. Update OpenSpec Config

- [x] 2.1 Update `openspec/config.yaml` context to reference `docs/rules.md` and instruct the AI to read it before generating any artifact
- [x] 2.2 Add the Rules Review Pass instruction to `openspec/config.yaml` — including the rule heuristic and the post-apply review flow
