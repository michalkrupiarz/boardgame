## State Logic (`src/state/GameState.ts`)

- [ ] 1.1 Update `getFoodThresholdForNextPopulation` to start at 8 (using `a=3, b=5`).
- [ ] 1.2 Verify that `getInitialState` doesn't need to be adjusted (current food 10).

## Rules Update (`docs/rules.md`)

- [ ] 2.1 Update section 8.3 with the new sequence (8, 13, 21, etc.).
