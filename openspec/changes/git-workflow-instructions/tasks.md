## 1. Configuration Update (`openspec/config.yaml`)

- [x] 1.1 Append the Git Workflow instructions to the `context` field in `openspec/config.yaml`.
- [x] 1.2 Verify the configuration is correctly formatted and reflects all user-requested rules.

## 2. Review Pass

- [x] 2.1 Finalize the `git-workflow-instructions` change with a Rules Review Pass.
- [x] 2.2 Archive the change once rules (if any) are approved.
